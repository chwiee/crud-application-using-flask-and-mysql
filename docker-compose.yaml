version: '2'

services:
  phonebook-mysql:
    container_name: db
    build:
      context: .
      dockerfile: Dockerfile-mysql
    restart: always

  phonebook-app:
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile-app
    depends_on:
      - phonebook-mysql
    ports:
      - "8181:8181"
    restart: always

  adminer:
    container_name: adminer
    image: adminer
    restart: always
    ports:
        - 10000:8080